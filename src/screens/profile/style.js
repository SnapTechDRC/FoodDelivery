import { StyleSheet, Text, View } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 5
    },
    imagesProfile: {
        width: 80,
        height: 80,
        borderRadius: 50
    }
})

export default styles